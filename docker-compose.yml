version: "3.8"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: regulatory
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  backend:
    build: ./backend
    depends_on:
      - postgres
    ports:
      - "8000:8000"